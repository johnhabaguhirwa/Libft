#include <string.h>

size_t	ft_wordsets(char *str, char sep)
{
	size_t = sets;
	size_t i;

	while(str[i])
	{
		while(str[i] && str[i] == sep)
			i++;
		while(str[i] && str[i] != sep)
			i++
		result++;
	}
	return(result);
}
